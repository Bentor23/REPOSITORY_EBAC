using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class componente3 : MonoBehaviour
{
    private void Awake()
    {
        
    }
    // Start is called before the first frame update
    void Start()
    {
        componente1.esfera.name = "bola";

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
